from helping_func import *


def task2(name_file: str):
    file = opening_file(name_file)
    file = file[1:]
    list_ship = []
    for row in file:
        list_ship = [row[0] + "-" + row[1], row[1]]
    # j =


    9, 4, 81, 78, 918
    for i in range(len(list_ship)):
        n_ship, num_ship = list_ship[i][0], list_ship[i][1]


    print(slov)



task2()